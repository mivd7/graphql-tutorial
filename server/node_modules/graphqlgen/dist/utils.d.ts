import { Language } from 'graphqlgen-json-schema';
import { NormalizedFile } from './parse';
import { FilesToTypesMap, InterfaceNamesToFile } from './introspection/types';
export declare function upperFirst(s: string): string;
/**
 * Support for different path notation
 *
 * './path/to/index.ts' => './path/to/index.ts'
 * './path/to' => './path/to/to.ts'
 * './path/to' => './path/to/index.ts'
 * './path/to/' => './path/to/index.ts'
 */
export declare function normalizeFilePath(filePath: string, language: Language): string;
/**
 * Create a map of interface names to the path of the file in which they're defined
 * The first evaluated interfaces are always the chosen ones
 */
export declare function getTypeToFileMapping(files: NormalizedFile[], filesToTypesMap: FilesToTypesMap): InterfaceNamesToFile;
export declare function replaceAll(str: string, search: string, replacement: string): string;
export declare function flatten(a: Array<any>, b: Array<any>): any[];
export declare function uniq(value: any, index: number, array: Array<any>): boolean;
