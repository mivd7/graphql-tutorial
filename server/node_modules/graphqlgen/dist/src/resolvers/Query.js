"use strict";
exports.__esModule = true;
exports.Query = {
    feed: function (parent, args, ctx) {
        return ctx.data.posts.filter(function (post) { return post.published; });
    },
    drafts: function (parent, args, ctx) {
        return ctx.data.posts.filter(function (post) { return !post.published; });
    },
    post: function (parent, args, ctx) {
        return ctx.data.posts.find(function (post) { return post.id === args.id; });
    }
};
