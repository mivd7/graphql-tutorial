"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var ts = require("typescript");
var path_1 = require("path");
var glob = require("glob");
var fs_1 = require("fs");
var relative = function (p) { return path_1.join(__dirname, p); };
test('compile typescript-yoga', function () {
    var typescriptYogaRootDir = relative('../../../../graphqlgen-templates/typescript-yoga');
    var compilerOptions = JSON.parse(fs_1.readFileSync(path_1.join(typescriptYogaRootDir, 'tsconfig.json')).toString()).compilerOptions;
    var fileNames = glob.sync(path_1.join(typescriptYogaRootDir, './**/*.ts'));
    var result = ts.createProgram(fileNames, __assign({}, compilerOptions, { sourceMap: false })).emit();
    // Expect 0 errors
    expect(result.diagnostics.length).toEqual(0);
});
//# sourceMappingURL=index.test.js.map